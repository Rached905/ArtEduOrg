<nav class="sb-sidenav accordion sb-sidenav-dark modern-sidenav" id="layoutSidenav_nav">
    <div class="sb-sidenav-menu">
        <div class="nav">
            <div class="sb-sidenav-menu-heading">Core</div>
            <a class="nav-link {{ app.request.attributes.get('_route') == 'app_vendeur' ? 'active' : '' }}" href="{{ path('app_vendeur') }}">
                <div class="sb-nav-link-icon"><i class="fas fa-home"></i></div>
                Accueil
            </a>

            <div class="sb-sidenav-menu-heading">Événements</div>
            <a class="nav-link {{ app.request.attributes.get('_route') == 'app_event_index' or app.request.attributes.get('_route') == 'app_event_show' ? 'active' : '' }}" href="{{ path('app_event_index') }}">
                <div class="sb-nav-link-icon"><i class="fas fa-calendar-alt"></i></div>
                Tous les Événements
            </a>

            <div class="sb-sidenav-menu-heading">Management</div>
            <a class="nav-link" href="{{ path('app_vendeur') }}">
                <div class="sb-nav-link-icon"><i class="fas fa-users"></i></div>
                Mon compte
            </a>
        </div>
    </div>
    <div class="sb-sidenav-footer modern-footer">
        <div class="small">Connecté en tant que:</div>
        <span class="user-name">{{ app.user ? app.user.fullname : 'Invité' }}</span>
    </div>
</nav>
